---
title: "Tickets"
description: "Configure an efficient ticket system for your server. - COMING SOON!"
icon: "ticket"
---

{/* <Warning>
    Configuring the ticket system requires the `manage_channels` permission
</Warning>

## Default Setup 
Command:`;ticket setup` <br />
Description: Automatically create a panel, ticket message, and category channel for your tickets.<br />

<Tabs>
    <Tab title="Example">
        <img src="/images/tickets/s23.png"/>
         <Tip>You might also wanna set a logs channel with `;ticket logs [channel]`</Tip>
    </Tab>
    <Tab title="Syntax">
       ```
       Syntax: ;ticket setup 
       Example: ;ticket setup
       ```
    </Tab>
</Tabs>


## Custom Setup 
This proccess is much more time consuming, if you want a quick ticket system in your server without having to do any prior configuration we recommend taking the default approach above.


### Ticket panel
Command:`;ticket panel` <br />
Description: Set the ticket panel message.<br />

<Tabs>
    <Tab title="Example">
        <img src="/images/tickets/s23.png"/>
         <Tip>You might also wanna set a logs channel with `;ticket logs [channel]`</Tip>
    </Tab>
    <Tab title="Syntax">
       ```
       Syntax: ;ticket panel (message)
       Example: ;ticket panel 1234567890
       ```
    </Tab>
</Tabs>


### Ticket SOMETHING....

<Tabs>
    <Tab title="Example">
        <img src="/images/tickets/s23.png"/>
    </Tab>
    <Tab title="Syntax">
       ```
       Syntax: ;ticket panel (message)
       Example: ;ticket panel 1234567890
       ```
    </Tab>
</Tabs> */}